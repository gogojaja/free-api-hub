# Free API Hub — AI 助理工作规则

> 项目状态与唯一事实来源：`交接文档.md`（工作断点，每任务完成由固化流程覆写）。
> 角色流程规则由 DevProjectTeamSkill（SKILL.md v8.2.0）统一提供，本文档不重复。
> 冲突裁决：本文档（项目级）优先于 skill（如评审输出用 CSV 禁 .xlsx）。

## 1. 项目环境约束

- workspace：`/Volumes/BR256G/free-api-hub`（历史文档写 KINGSTON120G 为漂移，以 BR256G 为准）
- 端口：chat :5080 / code :5081；禁止操作 5001/5002/5003
- 全库禁新 `.xlsx`；≥4K token 表格输出 CSV（UTF-8 BOM）；脚本一律 python3
- 干系人：段波（IP 所有人）；成本 <20 人*日（预警 16）

## 2. 项目铁律（最高优先级）

1. **先方案后执行**：接需求先梳理全局，输出主干方案 + 兜底方案，用户确认后再落地。
2. **方案验证铁律**：技术方案必须到 GitHub Issues/Discussions、Stack Overflow、官方文档验证，确认可用并贴引用来源，用户确认后落地；严禁凭训练数据臆想 API/功能。
3. **操作前影响评估**：修改入口脚本、导入链路、目录结构等核心文件，必须出具《操作影响评估表》（影响组件/风险/回滚），用户同意后执行。
4. **故障回滚优先**：服务异常默认从最新备份恢复，仅收到明确排查指令才检修。
5. **文件保护**：无用户明确指令，严禁删除/移动/重命名项目文件。
6. **阶段自动固化**：任务完成后自动执行 备份核心文件 → 更新文档版本 → 告知固化状态。

## 3. 通用工程规范

- 根因修复，拒绝临时绕过/注释屏蔽；同类错误重复 2 次切五维排查；全程禁 sudo
- 脚本内置 DEBUG 日志；业务脚本配套测试用例；>500 行模块化拆分；输出前语法自检
- 统一 `python -m venv`；锁定依赖版本与 Python 区间；适配 macOS
- 方案选型优先级：安全 > 稳定 > 成熟 > 便捷；优先 LTS、最小权限、拒绝实验工具
- 新会话接收交接文档后静待指令，不擅自操作
- 每轮回复末尾标注：**▶️ 下一步：** <可执行的后续操作>
- **每周监测例行**：新会话处理完当轮任务后，若距上次 `model_monitor.py --write` 运行 ≥7 天，则提示用户执行每周监测（价格→台账24/优惠→26/免费额度核验→27）并给出额度/促销变更摘要；会话中也可主动运行 `python3 scripts/model_monitor.py --write`

## 4. 会话复盘经验（持续积累）

1. **功能边界**：实验/废弃代码隔离不接生产路由；删除模块前先查 `from xxx import` 引用；未实现路由返回"开发中"提示。
2. **清理策略**：大型清理（卸载/删目录/重命名）先备份并记录在设计文档，磁盘释放数据留档核对。

---

**版本**：v1.1（精简版，2026-08-05）
