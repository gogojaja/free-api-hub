# 交接文档

## 0. 速览

- **项目目标**：Free API Hub（PRJ-001）——统一多免费 API 提供商网关，对外暴露 OpenAI 兼容接口，自动 failover；chat(:5080) / code(:5081) 双实例；应用 DevProjectTeamSkill v21 全生命周期治理。
- **当前阶段**：**项目已结项**（M0~M6 全生命周期闭环；M5 投产 Go-Live 评审 PASS，launchd 守护确认为 macOS TCC 外部卷平台限制不可用、生产用手动进程；M6 stage_close 收尾完成，17 台账归档 v0.8）。

## 1. 工作断点

> 本区由固化流程每次任务完成后覆写。
> **新模型/新会话启动，第一步必须先读 `交接文档.md` 全文**，从本区定位上一模型已完成/待办，未读交接文档前禁止读其他项目文档。

**最近固化时间**：2026-08-06
**阶段**：M6 收尾（完成，项目结项）+ GitHub 发布（完成）
**固化备注**：会话交接 v0.8——M6 stage_close 收尾完成：台账 10 成本（4.5 人*日 22.5% 利用率）/11 缺陷（D-S01~08 全整改）/09 进度（补 M3~M6）/13 审计（AUD-002）/14 门禁（收尾 PASS）/17 收尾归档（9 项交付物+经验总结）补全；交接文档更新终态；design v3.8 追加 M6；备份 backup/M6_v0.8_backup/ + git 提交。**GitHub 发布**：推送 github.com/gogojaja/free-api-hub（20 commits，HEAD 19c46fa）；历史明文密钥经 filter-repo 全历史清除（HTTPS 443 被网络干扰，改 SSH 推送；建议轮换曾明文流转的 4 类真实密钥）。**文档整理**：新增 README.md + docs/README.md 索引；评审报告 4 份 + 测试汇总移入 reports/（引用已同步）；opencode/vscode 配置合并为 docs/客户端配置.md；新增项目级 opencode.json（skills 指向全局 DevProjectTeamSkill v21.3.2）

### 已完成

1. **M0 启动阶段**：七环节闭环（init_kickoff → charter → stakeholder → scope → feasibility → ready → baseline），台账 17 CSV 初始化 v0.1。
2. **M1 需求阶段**：17 项需求登记 + 来源映射；存量 SRS/评审迁移 15 CSV；台账 08 追溯矩阵 29 行（断链率 0%）；需求审视 CHG-REQ-001（去3改3增4）；评审 PASS 7/INFO 3；基线 v0.2 固化。
3. **M2 架构阶段**：角色重置【架构设计师】；design.md v3.5 纳入架构资产（§9 七变更项设计，社区实践验证通过）；ADR v1.0 决策记录 7 条（docs/ADR.md）；架构追溯矩阵扩展至 33 行（断链 0%）；台账 09/16 同步；基线 v0.3 固化。
4. **M3 开发阶段**：CHG-REQ-001 7 变更项全部落地代码并测试（每项独立提交）：
   - NEW-004 配置快照（scripts/backup_config.py + _snapshot_config，TC-B1~B3）
   - NEW-001 重试+指数退避（call_api/_retry_delay/_retry_wait，429 Retry-After 优先，TC-R1~R3）
   - NEW-003 429 规范化（server.py _apply_rate_limit/_error_response + RateLimiter，TC-L1~L3）
   - CONFIG-002 配置热加载（_maybe_reload mtime+5s TTL，TC-H1~H3）
   - DEGRADE-001 失败率熔断（CircuitBreaker 滑动窗口>25%开路 + 429/5xx 差异化冷却，TC-D1~D4）
   - FAILOVER-003 渐进恢复（should_allow 半开 20% 试探 + 连续 2 次成功全量恢复，TC-F1~F4）
   - NEW-002 可观测性指标（GET /metrics Prometheus 文本，认证=公开只读，TC-M1~M4）
   单元测试 7 文件 24 用例全过；回归 12/12 通过（5080 实例重启生效）；台账 15/16 更新。
5. **M4 测试阶段**：集成回归扩展——回归套件 12→17 用例：
   - TC-15 `/metrics` 公开只读（HTTP 实网，NEW-002）
   - TC-16 `/metrics` 指标完整性（fah_requests_total/fah_circuit_breaker_state/fah_available_providers/fah_http_latency_seconds）
   - TC-17 熔断器详情结构（circuit_breakers 每项 state/cooldown/failure_count）
   - TC-18 全生命周期链路（进程内：重试耗尽→熔断OPEN→冷却→HALF_OPEN→渐进恢复→CLOSED）
   - TC-19 429 规范化链路（进程内：Retry-After 等待→耗尽→status_code=429 透传）
    新增 `tests/run_all_tests.py` 统一调度 8 套件 41 用例 ALL PASS，汇总表 `reports/测试汇总_M4_集成回归.csv`；design.md v3.6；台账 15/16 更新。
6. **M5 投产阶段**：
   - Go-Live 评审 PASS：需求 17 项全 ✅、CHG-REQ-001 7 变更项落地、单元 24 + 集成回归 41 用例全过、安全门禁（认证拒绝 401/无明文 Key）通过 → `reports/评审报告_投产评审_v1.0.csv`
   - launchd 守护安装：plist 模板已备份（backup/M5_v0.5_backup/）；安装后启动失败 EX_CONFIG 78，定位为 **macOS TCC 外部卷权限**——launchd 进程读 `/Volumes` 的 venv/pyvenv.cfg 被拒（Operation not permitted），需为 Python 解释器授予「完全磁盘访问」（TCC 按二进制授权，`tccutil add` 已移除，仅 GUI）
   - 回滚：卸载 launchd，恢复 5080 手动进程（3/3 provider 可用）；风险登记 RISK-004；新增 `docs/运维手册.md`
   - **根因确认（平台限制）**：macOS 26.5.2 将外部卷访问归为 TCC「可移动卷」类别，GUI 不提供 CLI 二进制的授权开关（完全磁盘访问已授权但不覆盖外部卷，`/bin/sh` 系统二进制默认豁免）；launchd 直接 spawn 的 python 无法读 `/Volumes` → **launchd 守护确认不可用**，生产以手动进程运行（Terminal 启动继承可移动卷权限）
   - **M5 闭环**：台账 03/12/14/15 更新（M5 ✅，RISK-004 已缓解），运维手册 §3 记录启动/停止标准方式与未来内部卷启用路径
7. **M6 收尾阶段（完成，项目结项）**：
   - stage_close 门禁 PASS（台账 14 收尾行）
   - 台账补全：10 成本（4.5 人*日 22.5% 利用率，无超支）、11 缺陷（D-S01~08 全整改）、09 进度（补 M3~M6 快照 9 行）、13 审计（AUD-002 launchd 留痕）、17 收尾归档（9 项交付物+经验总结）
   - design.md v3.8 追加 M6 收尾记录；交接文档更新终态；固化 backup/M6_v0.8_backup/ + git 提交

### 进行中

- 无（项目已结项）。

### 待办

- 无（M0~M6 全生命周期闭环）。后续为迭代维护：`docs/运维手册.md` 为准。

### 阻塞

- 无（RISK-004 launchd TCC 已缓解为手动进程投产）。

### 台账指针

主台账 CSV 路径：`/Volumes/KINGSTON120G/free-api-hub/台账/`（17 个 NN_ 前缀 CSV，全部有数据）　基线版本：v0.8（M6 收尾完成，项目结项）

## 2. 关键文件索引

| 文件 | 用途 |
|------|------|
| `README.md` | 项目总览 + 文档索引（先读） |
| `docs/README.md` | docs 目录索引 |
| `docs/requirements.md` | 需求文档（17 项，全 ✅） |
| `docs/design.md` | 设计文档（v3.8，含 §9 CHG-REQ-001 七变更项设计 + M4 集成回归 + M5 投产 + M6 收尾） |
| `docs/运维手册.md` | 运维手册（启动/停止/健康/指标/launchd 受限说明/故障恢复） |
| `docs/客户端配置.md` | AI 客户端（opencode/VSCode）模型路由配置 |
| `docs/ADR.md` | 架构决策记录（v1.0，ADR-001~007） |
| `docs/整改方案_严重缺陷_v1.0.md` | 严重缺陷整改方案（D-S01~08，P0 已整改） |
| `reports/评审报告_投产评审_v1.0.csv` | M5 投产评审（需求/测试/安全全 PASS） |
| `requirements/` | 需求工作目录（17 CSV：收集清单/来源映射/SRS 10 章/评审 5 Sheet） |
| `reports/评审报告_需求阶段_v1.0_数据.csv` | 需求阶段评审（PASS 7/INFO 3） |
| `reports/评审报告_需求审视_v1.0_逐项.csv` | 需求审视（去3改3增4） |
| `reports/评审报告_架构评估_CHG-REQ-001.csv` | CHG-REQ-001 架构评估（7 变更项） |
| `台账/` | 主台账 17 个 CSV（基线 v0.8，项目结项） |
| `backup/legacy_xlsx/` | 存量 xlsx 归档（SRS/评审原文件） |
| `backup/台账_v0.3_backup/` | v0.3 台账备份 |
| `backup/handover_v0.3.zip` | 交接快照包 |
| `src/server.py` + `src/gateway.py` | 网关实现 |
| `tests/test_regression.py` | 集成回归测试（17 用例，本地 5080） |
| `tests/run_all_tests.py` | 统一测试调度（8 套件 41 用例 + CSV 汇总） |
| `tests/test_retry.py` 等 7 文件 | M3 变更项单元测试（24 用例） |
| `reports/测试汇总_M4_集成回归.csv` | M4 集成回归汇总表（UTF-8 BOM） |
| `logs/chat_server.log` | 5080 服务日志 |

## 3. 约定与铁律

- 新会话第一步先读本文件「工作断点」，未读前禁止读其他项目文档。
- 每次原子修改任务完成后固化并 git 提交；全库禁止新 `.xlsx`；≥4K token 表格输出 CSV（UTF-8 BOM），`head -5` 预览禁止全文 cat。
- 服务端口 5080/5081（chat/code），禁止操作主环境端口 5001/5002/5003。
- 环境：macOS；Python venv 已建；无 bash 依赖，脚本均用 python3。
- 干系人：段波（IP 所有人，高权高利-重点管理）；成本预算 <20 人*日（预警 16 人*日）。

---

## 4. 新会话开场话术（handover_export 生成）

> 你好。按 DevProjectTeamSkill 全生命周期治理，Free API Hub（PRJ-001）当前进度：
>
> - **已完成**：M0 启动（台账 v0.1）+ M1 需求（台账 v0.2，17 项登记、追溯矩阵 0% 断链、审视变更 CHG-REQ-001）+ M2 架构（台账 v0.3，design.md v3.5 §9 七变更项设计、ADR v1.0、追溯矩阵 33 行）+ M3 开发（台账 v0.4，7 变更项全部落地，单元 24/24 + 回归 12/12 通过）+ M4 测试（台账 v0.5，回归套件 12→17 用例，统一调度 8 套件 41 用例 ALL PASS）+ M5 投产（台账 v0.6，Go-Live 评审 PASS；launchd 守护确认为 TCC 平台限制，生产手动进程）+ **M6 收尾（台账 v0.8，stage_close PASS，项目结项）**。
> - **当前阶段**：项目已结项（M0~M6 全生命周期闭环）。
> - **待办**：无；后续为迭代维护（`docs/运维手册.md`）。
> - **关键台账**：`台账/` 17 CSV（基线 v0.8，全部有数据）；评审报告 4 份；设计 docs/design.md v3.8 + docs/ADR.md v1.0 + docs/运维手册.md。
> - **约束**：端口 5080/5081；禁 .xlsx；每任务完成固化+git 提交；成本 <20 人*日。
>
> 请先读本 `交接文档.md`「工作断点」，静待我的指令。
