# 交接文档

## 0. 速览

- **项目目标**：Free API Hub（PRJ-001）——统一多免费 API 提供商网关，对外暴露 OpenAI 兼容接口，自动 failover；chat(:5080) / code(:5081) 双实例；应用 DevProjectTeamSkill v21 全生命周期治理。
- **当前阶段**：M1 需求阶段完成（台账基线 v0.2），遗留待办已清零，待进入架构阶段。
- **下一步唯一动作**：重置角色加载【架构设计师】，将 design.md v3.4 + CHG-REQ-001 架构评估（7 变更项）纳入架构资产、ADR 决策记录与架构追溯矩阵。

## 1. 工作断点

> 本区由固化流程每次任务完成后覆写。
> **新模型/新会话启动，第一步必须先读 `交接文档.md` 全文**，从本区定位上一模型已完成/待办，未读交接文档前禁止读其他项目文档。

**最近固化时间**：2026-08-05
**阶段**：M1 需求（收尾）
**固化备注**：会话交接 v0.2——M0+M1 闭环、遗留待办清零、交接快照打包

### 已完成

1. **M0 启动阶段**：七环节闭环（init_kickoff → charter → stakeholder → scope → feasibility → ready → baseline），台账 17 CSV 初始化 v0.1。
2. **M1 需求阶段**：17 项需求登记 + 来源映射；存量 SRS/评审迁移 15 CSV；台账 08 追溯矩阵 29 行（断链率 0%）；需求审视 CHG-REQ-001（去3改3增4）；评审 PASS 7/INFO 3；基线 v0.2 固化。
3. **遗留待办清零**：存量 xlsx 归档 backup/legacy_xlsx/（项目根 xlsx 0 残留，AUD-001 留痕）；CHG-REQ-001 架构评估完成（评审报告_架构评估_CHG-REQ-001.csv，7 变更项方案确认）；干系人（段波）+ 成本（<20 人*日）补录。
4. **交接快照**：backup/handover_v0.2.zip（17 台账 CSV + 3 评审报告 + 交接文档，23 文件）。

### 进行中

- 无（当前会话收尾，待交接）。

### 待办

1. **M2 架构阶段**：重置角色加载【架构设计师】；design.md v3.4 纳入架构资产；CHG-REQ-001 7 变更项实现方案（低难度 4：CONFIG-002/NEW-001/NEW-003/NEW-004；中难度 3：DEGRADE-001/FAILOVER-003/NEW-002）；ADR 决策记录 + 架构追溯矩阵 + 架构基线固化。
2. **M3 开发阶段**：CHG-REQ-001 变更项落地实现（src/gateway.py + server.py）。
3. **M4 测试阶段**：回归测试扩展（新增用例覆盖重试/熔断/429/指标）。
4. **M5 投产阶段**：Go-Live 评审 + launchd 守护核验 + 运维交接。

### 阻塞

- 无。

### 台账指针

主台账 CSV 路径：`/Volumes/KINGSTON120G/free-api-hub/台账/`（17 个 NN_ 前缀 CSV，当前有数据：01/02/03/04/05/06/07/08/12/13/14/15）　基线版本：v0.2　交接包：backup/handover_v0.2.zip

## 2. 关键文件索引

| 文件 | 用途 |
|------|------|
| `docs/requirements.md` | 需求文档（17 项，全 ✅） |
| `docs/design.md` | 设计文档（v3.4） |
| `docs/整改方案_严重缺陷_v1.0.md` | 严重缺陷整改方案（D-S01~08，P0 已整改） |
| `requirements/` | 需求工作目录（17 CSV：收集清单/来源映射/SRS 10 章/评审 5 Sheet） |
| `评审报告_需求阶段_v1.0_数据.csv` | 需求阶段评审（PASS 7/INFO 3） |
| `评审报告_需求审视_v1.0_逐项.csv` | 需求审视（去3改3增4） |
| `评审报告_架构评估_CHG-REQ-001.csv` | CHG-REQ-001 架构评估（7 变更项） |
| `台账/` | 主台账 17 个 CSV（基线 v0.2） |
| `backup/legacy_xlsx/` | 存量 xlsx 归档（SRS/评审原文件） |
| `backup/台账_v0.2_backup/` | v0.2 台账备份 |
| `backup/handover_v0.2.zip` | 交接快照包 |
| `src/server.py` + `src/gateway.py` | 网关实现 |
| `tests/test_regression.py` | 回归测试（12 用例） |

## 3. 约定与铁律

- 新会话第一步先读本文件「工作断点」，未读前禁止读其他项目文档。
- 每次原子修改任务完成后固化并 git 提交；全库禁止新 `.xlsx`；≥4K token 表格输出 CSV（UTF-8 BOM），`head -5` 预览禁止全文 cat。
- 服务端口 5080/5081（chat/code），禁止操作主环境端口 5001/5002/5003。
- 环境：macOS；Python venv 已建；无 bash 依赖，脚本均用 python3。
- 干系人：段波（IP 所有人，高权高利-重点管理）；成本预算 <20 人*日（预警 16 人*日）。

---

## 4. 新会话开场话术（handover_export 生成）

> 你好。按 DevProjectTeamSkill 全生命周期治理，Free API Hub（PRJ-001）当前进度：
>
> - **已完成**：M0 启动（台账 v0.1）+ M1 需求（台账 v0.2，17 项登记、追溯矩阵 0% 断链、审视变更 CHG-REQ-001）+ 遗留待办清零。
> - **当前阶段**：M2 架构（待启动）。
> - **待办**：加载【架构设计师】→ design.md v3.4 纳入架构资产 → CHG-REQ-001 7 变更项方案/ADR → 架构基线固化。
> - **关键台账**：`台账/` 17 CSV（基线 v0.2）；评审报告 3 份；交接包 `backup/handover_v0.2.zip`。
> - **约束**：端口 5080/5081；禁 .xlsx；每任务完成固化+git 提交；成本 <20 人*日。
>
> 请先读本 `交接文档.md`「工作断点」，静待我的指令。
