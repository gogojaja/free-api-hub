# 交接文档

## 0. 速览

- **项目目标**：Free API Hub（PRJ-001）——统一多免费 API 提供商网关，对外暴露 OpenAI 兼容接口，自动 failover；chat(:5080) / code(:5081) 双实例；应用 DevProjectTeamSkill v21 全生命周期治理。
- **当前阶段**：M0 启动阶段完成（台账基线 v0.1），待进入需求阶段。
- **下一步唯一动作**：重置角色加载【需求分析师】，将存量需求 17 项补录需求追溯矩阵，或迁移存量 xlsx。

## 1. 工作断点

> 本区由固化流程每次任务完成后覆写。
> **新模型/新会话启动，第一步必须先读 `交接文档.md` 全文**，从本区定位上一模型已完成/待办，未读交接文档前禁止读其他项目文档。

**最近固化时间**：2026-08-05
**阶段**：M0 启动
**固化备注**：启动七环节闭环，台账 17 CSV 基线 v0.1

### 已完成

1. M0 启动七环节全部闭环：init_kickoff（PRJ-001）→ create_charter → register_stakeholder → define_scope_prelim（16 行范围基准）→ assess_feasibility（Go）→ check_ready（Go）→ init_baseline。
2. 台账初始化：`台账/` 17 个 CSV 全部创建（UTF-8 BOM），基线 v0.1；已写入 01 启动组 / 02 范围基准 / 03 进度基准 / 04 成本基准 / 05 质量基准 / 12 风险问题台账（3 初始风险）/ 15 执行记录（7 环节）。
3. 启动完成报告输出：Go 判定，移交需求阶段。

### 进行中

- 无（本阶段收尾完成）。

### 待办

1. 需求阶段：重置角色加载【需求分析师】，存量需求 17 项补录 08 需求追溯矩阵；或走需求分析流程。
2. 存量 `SRS_v1.0.xlsx` / `评审报告_需求评审_v1.0.xlsx` 迁移 CSV（需 `change_audit` 审计，待授权）。
3. 干系人真实名单、工时成本数据待补录（当前为占位）。
4. 架构/开发/测试/投产阶段按台账事实补登记。

### 阻塞

- 无。

### 台账指针

主台账 CSV 路径：`/Volumes/KINGSTON120G/free-api-hub/台账/`（17 个 NN_ 前缀 CSV）　基线版本：v0.1

## 2. 关键文件索引

| 文件 | 用途 |
|------|------|
| `docs/requirements.md` | 需求文档（17 项，全 ✅） |
| `docs/design.md` | 设计文档（v3.4） |
| `需求规格说明书_SRS_v1.0.xlsx` | 存量 SRS（旧体系，待迁移 CSV） |
| `评审报告_需求评审_v1.0.xlsx` | 存量评审报告（旧体系，待迁移 CSV） |
| `台账/` | 主台账 17 个 CSV（基线 v0.1） |
| `src/server.py` + `src/gateway.py` | 网关实现 |
| `tests/test_regression.py` | 回归测试（12 用例） |

## 3. 约定与铁律

- 新会话第一步先读本文件「工作断点」，未读前禁止读其他项目文档。
- 每次原子修改任务完成后固化并 git 提交；全库禁止新 `.xlsx`；≥4K token 表格输出 CSV（UTF-8 BOM），`head -5` 预览禁止全文 cat。
- 服务端口 5080/5081（chat/code），禁止操作主环境端口 5001/5002/5003。
- 环境：macOS；Python venv 已建；无 bash 依赖，脚本均用 python3。
