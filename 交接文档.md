# 交接文档

## 0. 速览

- **项目定位（铁律）**：本项目是整个项目群的基础设施，以稳定为第一要求。职责：维护可用模型清单、提供商余额/费用/优惠信息、保证项目群使用最低价最适合的模型。**禁止**：模型路由、请求聚合、流量转发。只做最基础的模型列表可用性服务。
- **当前阶段**：**网关已删除，项目重新定位为模型清单管理基础设施**。保留 config/providers.yaml（各提供商独立配置）+ scripts/（监控脚本）。OpenCode 中各提供商已直连配置（zhipu/siliconflow/bailian/openrouter/moonshot/modelscope/longcat/mimo/tencent）。

## 1. 工作断点

> 本区由固化流程每次任务完成后覆写。
> **新模型/新会话启动，第一步必须先读 `交接文档.md` 全文**，从本区定位上一模型已完成/待办，未读交接文档前禁止读其他项目文档。

**最近固化时间**：2026-09-01
**阶段**：项目重新定位——删除网关，回归模型清单管理基础设施
**固化备注**：(1) 删除全部网关代码（src/gateway.py/routing.py/server.py），端口 5080 已释放。(2) 项目铁律写入 AGENTS.md：禁止模型路由/请求聚合/流量转发，只做模型列表可用性服务。(3) OpenCode 配置清理：移除 free-api-hub provider（指向 localhost:5080），新增 longcat/mimo/tencent 三个直连 provider。(4) 各提供商 API key 文件已创建于 ~/.config/opencode/。

### 已完成

1. **M0 启动阶段**：七环节闭环（init_kickoff → charter → stakeholder → scope → feasibility → ready → baseline），台账 17 CSV 初始化 v0.1。
2. **M1 需求阶段**：17 项需求登记 + 来源映射；存量 SRS/评审迁移 15 CSV；台账 08 追溯矩阵 29 行（断链率 0%）；需求审视 CHG-REQ-001（去3改3增4）；评审 PASS 7/INFO 3；基线 v0.2 固化。
3. **M2 架构阶段**：角色重置【架构设计师】；design.md v3.5 纳入架构资产（§9 七变更项设计，社区实践验证通过）；ADR v1.0 决策记录 7 条（docs/ADR.md）；架构追溯矩阵扩展至 33 行（断链 0%）；台账 09/16 同步；基线 v0.3 固化。
4. **M3 开发阶段**：CHG-REQ-001 7 变更项全部落地代码并测试（每项独立提交）：
   - NEW-004 配置快照（scripts/backup_config.py + _snapshot_config，TC-B1~B3）
   - NEW-001 重试+指数退避（call_api/_retry_delay/_retry_wait，429 Retry-After 优先，TC-R1~R3）
   - NEW-003 429 规范化（server.py _apply_rate_limit/_error_response + RateLimiter，TC-L1~L3）
   - CONFIG-002 配置热加载（_maybe_reload mtime+5s TTL，TC-H1~H3）
   - DEGRADE-001 失败率熔断（CircuitBreaker 滑动窗口>25%开路 + 429/5xx 差异化冷却，TC-D1~D4）
   - FAILOVER-003 渐进恢复（should_allow 半开 20% 试探 + 连续 2 次成功全量恢复，TC-F1~F4）
   - NEW-002 可观测性指标（GET /metrics Prometheus 文本，认证=公开只读，TC-M1~M4）
   单元测试 7 文件 24 用例全过；回归 12/12 通过（5080 实例重启生效）；台账 15/16 更新。
5. **M4 测试阶段**：集成回归扩展——回归套件 12→17 用例：
   - TC-15 `/metrics` 公开只读（HTTP 实网，NEW-002）
   - TC-16 `/metrics` 指标完整性（fah_requests_total/fah_circuit_breaker_state/fah_available_providers/fah_http_latency_seconds）
   - TC-17 熔断器详情结构（circuit_breakers 每项 state/cooldown/failure_count）
   - TC-18 全生命周期链路（进程内：重试耗尽→熔断OPEN→冷却→HALF_OPEN→渐进恢复→CLOSED）
   - TC-19 429 规范化链路（进程内：Retry-After 等待→耗尽→status_code=429 透传）
    新增 `tests/run_all_tests.py` 统一调度 8 套件 41 用例 ALL PASS，汇总表 `reports/测试汇总_M4_集成回归.csv`；design.md v3.6；台账 15/16 更新。
6. **M5 投产阶段**：
   - Go-Live 评审 PASS：需求 17 项全 ✅、CHG-REQ-001 7 变更项落地、单元 24 + 集成回归 41 用例全过、安全门禁（认证拒绝 401/无明文 Key）通过 → `reports/评审报告_投产评审_v1.0.csv`
   - launchd 守护安装：plist 模板已备份（backup/M5_v0.5_backup/）；安装后启动失败 EX_CONFIG 78，定位为 **macOS TCC 外部卷权限**——launchd 进程读 `/Volumes` 的 venv/pyvenv.cfg 被拒（Operation not permitted），需为 Python 解释器授予「完全磁盘访问」（TCC 按二进制授权，`tccutil add` 已移除，仅 GUI）
   - 回滚：卸载 launchd，恢复 5080 手动进程（3/3 provider 可用）；风险登记 RISK-004；新增 `docs/运维手册.md`
   - **根因确认（平台限制）**：macOS 26.5.2 将外部卷访问归为 TCC「可移动卷」类别，GUI 不提供 CLI 二进制的授权开关（完全磁盘访问已授权但不覆盖外部卷，`/bin/sh` 系统二进制默认豁免）；launchd 直接 spawn 的 python 无法读 `/Volumes` → **launchd 守护确认不可用**，生产以手动进程运行（Terminal 启动继承可移动卷权限）
   - **M5 闭环**：台账 03/12/14/15 更新（M5 ✅，RISK-004 已缓解），运维手册 §3 记录启动/停止标准方式与未来内部卷启用路径
7. **M6 收尾阶段（完成，项目结项）**：
   - stage_close 门禁 PASS（台账 14 收尾行）
   - 台账补全：10 成本（4.5 人*日 22.5% 利用率，无超支）、11 缺陷（D-S01~08 全整改）、09 进度（补 M3~M6 快照 9 行）、13 审计（AUD-002 launchd 留痕）、17 收尾归档（9 项交付物+经验总结）
   - design.md v3.8 追加 M6 收尾记录；交接文档更新终态；固化 backup/M6_v0.8_backup/ + git 提交

### 迭代维护 M7（智能路由，2026-08-29）

- **已完成（方案 + 评审 + 实施 + 单元验证）**：智能路由特性落地——ADR-008 统一模型别名层 + ADR-009 能力打分选路（叠加式、非破坏性）。
  - 代码：`src/routing.py`（新增）；`src/gateway.py`（`_resolve_route` 接入 `call_api`、`list_models` 别名展示、`_record_latency` 按 provider 维护 EWMA）；配置 `config/chat.yaml`/`code.yaml`/`providers.yaml.example` 增 `routing` 段 + `capabilities`。
  - 测试：`tests/test_routing.py`（9 用例）+ 注册 `tests/run_all_tests.py`；9 路由 + 24 既有单元全 PASS。
  - 文档：`docs/design_smart_routing.md`、`docs/ADR.md`（ADR-008/009）、`reports/评审报告_智能路由方案_v1.0_逐原则.csv`（PASS×9 / INFO×1，INFO 已补追溯矩阵）、`docs/requirements.md` §6.1、追溯矩阵 `08`/`16` 补 REQ-SR-001/002。
  - **opencode 保护**：`hide_raw=false` + 未知/占位 model（如 `free-api-hub`）→ 现状全池 failover；`/v1/models` 保留裸下游模型名，客户端模型列表不受影响。
  - **待活实例激活**：需含 API Key 的常态启动 `python3 src/server.py --config config/chat.yaml`（本会话测试 shell 无 Key，已杀掉无 Key 残实例，5080 恢复未运行原状）。

### 进行中

- 无（模型清单扩展已完成，5080 运行中 40 模型/3 别名）。

### 待办

- 无紧急待办。
- **可选后续**：①每周监测首次正式运行 `python3 scripts/model_monitor.py --write`；②OpenRouter 免费模型 429 限流、付费模型需充值 credits；③各提供商余额/优惠变动时更新 `config/providers.yaml` 和台账。

### 阻塞

- 无（RISK-004 launchd TCC 已缓解为手动进程投产）。

### 台账指针

主台账 CSV 路径：`/Volumes/BR256G/free-api-hub/台账/`（17 个 NN_ 前缀 CSV，全部有数据）　基线版本：v0.10（模型清单扩展 + 监测工具可复用化）

## 2. 关键文件索引

| 文件 | 用途 |
|------|------|
| `README.md` | 项目总览 + 文档索引（先读） |
| `AGENTS.md` | AI 助理工作规则（含铁律：禁止路由/聚合/转发） |
| `docs/README.md` | docs 目录索引 |
| `docs/requirements.md` | 需求文档（17 项，全 ✅） |
| `docs/design.md` | 设计文档（v3.8，含 CHG-REQ-001 七变更项 + M4~M6 记录） |
| `docs/运维手册.md` | 运维手册（启动/停止/健康/指标） |
| `docs/ADR.md` | 架构决策记录（v1.0，ADR-001~007） |
| `config/providers.yaml` | 各提供商独立配置（智谱/硅基/百炼/OpenRouter/美团/小米/腾讯） |
| `config/chat.yaml` | 路由配置备份（网关已删除，仅供参考） |
| `scripts/api_monitor.py` | 健康检查工具（`--check`/`--gateway`/`--status`） |
| `scripts/daily_api_recommendation.py` | 每日成本优化报告 |
| `scripts/model_monitor.py` | 模型监控（`--write` 写入台账） |
| `台账/` | 主台账 17+ CSV（模型价格/免费额度/日报推送/费用趋势） |
| `reports/` | 评审报告 + 测试汇总 |
| `tests/` | 单元测试 + 集成回归（网关已删除，测试仅供参考） |

## 3. 约定与铁律

- 新会话第一步先读本文件「工作断点」，未读前禁止读其他项目文档。
- 每次原子修改任务完成后固化并 git 提交；全库禁止新 `.xlsx`；≥4K token 表格输出 CSV（UTF-8 BOM），`head -5` 预览禁止全文 cat。
- 本项目无网关服务，不监听任何端口。各提供商在 OpenCode 中独立配置，不做聚合。
- 环境：macOS；Python venv 已建（venv/）；脚本均用 python3。
- 干系人：段波（IP 所有人，高权高利-重点管理）；成本预算 <20 人*日（预警 16 人*日）。
- **项目铁律（AGENTS.md 第 0 条）**：禁止模型路由、请求聚合、流量转发。只做最基础的模型列表可用性服务。

---

## 4. 新会话开场话术（handover_export 生成）

> 你好。Free API Hub 当前状态：
>
> - **项目定位（铁律）**：整个项目群的基础设施，以稳定为第一要求。职责：维护可用模型清单、提供商余额/费用/优惠信息、保证项目群使用最低价最适合的模型。**禁止**：模型路由、请求聚合、流量转发。
> - **当前阶段**：网关已删除（2026-09-01），项目重新定位为模型清单管理基础设施。
> - **已配置提供商**：智谱 GLM（free+recharged）、硅基流动（free+recharged）、百炼（充值）、OpenRouter（free+low）、Moonshot、ModelScope（free）、美团 LongCat（充值）、小米 MiMo（充值）、腾讯 TokenHub（充值）。
> - **关键文件**：`config/providers.yaml`（提供商配置）、`scripts/api_monitor.py`（健康检查）、`scripts/model_monitor.py`（模型监控）、`AGENTS.md`（铁律）。
> - **约束**：禁 .xlsx；每任务完成固化+git 提交；成本 <20 人*日。
>
> 请先读本 `交接文档.md`「工作断点」，静待我的指令。
